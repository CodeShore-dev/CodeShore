# Puppeteer 使用的 Chrome/Chromium 執行檔路徑（爬蟲用瀏覽器自動化）
# 範例（WSL 下呼叫 Windows Chrome）：/mnt/c/Program Files/Google/Chrome/Application/chrome.exe
PUPPETEER_EXECUTABLE_PATH=
# Chrome profile 目錄。WSL 下呼叫 Windows Chrome 時必填,且須為 Windows 路徑
# 範例：C:\Users\<you>\AppData\Local\Temp\puppeteer-wsl-profile
PUPPETEER_USER_DATA_DIR=

# 被限流(HTTP 429)/ 被 Cloudflare 擋下後的退讓重試(單位:分鐘,可含小數)
# 預設 5 分鐘起、每次重試再多 5 分鐘(5m → 10m → 15m …),不設上限、一直重試到成功
CRAWL_BACKOFF_INITIAL_MINUTES=
CRAWL_BACKOFF_INCREMENT_MINUTES=
# 單次等待上限(分鐘);留空 = 不設上限
CRAWL_BACKOFF_MAX_DELAY_MINUTES=
# 最多重試幾次;留空 = 不設上限
CRAWL_BACKOFF_MAX_RETRIES=
